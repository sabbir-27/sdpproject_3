name: smartshop_connect
description: A premium glassmorphism-based Flutter UI starter for SmartShop Connect.
publish_to: "none"

version: 1.0.0+1

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  fl_chart: ^0.66.0
  # 🌈 UI & Animation
  google_fonts: ^6.2.1
  flutter_animate: ^4.5.0
  glassmorphism: ^3.0.0
  lottie: ^3.1.0

  # 🗺 Map (for Police screen)
  flutter_map: ^6.1.0
  latlong2: ^0.9.1

  # 💬 Chat Bubble & UI helpers
  intl: ^0.19.0

  # 🚀 State management (lightweight)
  provider: ^6.1.2
  
  # 📷 Image & File selection
  file_picker: ^8.0.0 # Updated to a newer version

  # 📷 QR Code
  qr_flutter: ^4.1.0

  # 🌐 Networking
  http: ^1.2.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true

  assets:
    - assets/images/
    - assets/icons/
    - assets/animations/

  # fonts:
  #   - family: Poppins
  #     fonts:
  #       - asset: assets/fonts/Poppins-Regular.ttf
  #       - asset: assets/fonts/Poppins-Medium.ttf
  #       - asset: assets/fonts/Poppins-SemiBold.ttf
  #         weight: 600
  #       - asset: assets/fonts/Poppins-Bold.ttf
  #         weight: 700
